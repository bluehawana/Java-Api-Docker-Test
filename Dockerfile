# Use an officialSure, here's a basic `Dockerfile` Open aJDK runtime as a basic `Dockerfile` for a Spring Boot a basic `Dockerfile` for a Spring Boot parent image
FROM for a Spring Boot application:

```Dockerfile
# Use an official OpenJDK runtime as a application:

```Dockerfile
# Use an official OpenJDK runtime as a openjdk:17-jdk application:

```Dockerfile
# Use an official OpenJDK runtime as a-slim

# Set the working directory in the container
WORK parentDIR image
FROM openjdk:17-jdk-slim

# Set the working directory in the container parent image
FROM openjdk:17-jdk-slim

# Set the working directory in the container parent image
FROM openjdk:17-jdk-slim

# Set the working / directory in the containerapp

# Copy the


WORKDIR /app

# Copy the application's jar file to the container
COPY target/notes-app.jar /app/notes-app.jar

# Expose the port the application runs on
EXPOSE 8080

# Run the jar file
ENTRYPOINT ["java", "-jar", "notes application's jar file to the container
COPY target/notes-app.jar /app/notes-app.jar

# Expose the port the application runs on
EXPOSE 8080

# Run the jar file
ENTRYPOINT ["java", "-jar", "notes-app.jar"]